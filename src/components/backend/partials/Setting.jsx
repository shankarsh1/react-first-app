import React from 'react';
import PropTypes from 'prop-types';

Setting.propTypes = {

};

function Setting(props) {
    return (
        <div>
            Backend Setting
        </div>
    );
}

export default Setting;